#
# Architechture dependent part : FX1000
#
CC = fccpx
CXX = FCCpx
FC = frtpx

COMMON_C_SRC += Matrix_SSL2.c

#SSL2FLAGS = -SSL2
SSL2FLAGS += -SSL2BLAMP -DSSL2

CXXFLAGS += -O3 -std=c++11 $(SSL2FLAGS) -DWITH_SSL2
CFLAGS += -O3 -std=c11 $(SSL2FLAGS) -DWITH_SSL2
FFLAGS += -O3 $(SSL2FLAGS) -DWITH_SSL2
LDFLAGS += -mlcmain=main $(SSL2FLAGS)

include ../standalone/Make.common
